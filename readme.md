# Blog

Rename the file .env-example by .env and fill your data
We are using mysql with the default port 3306 rather than mariaDB in xampp
Running the init.sql file in /app/models/sql folder
Put all files in /htdocs/blog
Create folder /app/uploads
Open apache on xampp
Open http://locahost/blog for running
